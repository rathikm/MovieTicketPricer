{
  "name": "movieticketpricer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sudo node server/index.js",
    "server": "sudo node server/index.js",
    "client": "sudo npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "sudo npm run build --prefix client",
    "heroku_postbuild": "sudo cd client && sudo npm install --only=dev && sudo npm install && sudo npm install react-scripts --save && sudo npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^3.2.4",
    "axios": "^1.3.4",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "request": "^2.88.2"
  }
}
